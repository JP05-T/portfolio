This project is a simple personal educational portfolio built with
Pyramid (Python) and Bootstrap 5.

To run locally:

    pip install -e .
    pserve development.ini

Then open http://localhost:6543
